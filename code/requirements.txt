# Core dependencies — required on every platform.
# These have prebuilt wheels for all major OS/arch combinations including
# Windows ARM64, so `pip install -r requirements.txt` always succeeds.
numpy>=2.0
pandas>=2.0
matplotlib>=3.8
python-pptx>=1.0
pytest>=8.0

# WebUI dependencies live in requirements-webui.txt — they are installed
# automatically by `dev.py setup` as a best-effort step, but their
# absence does NOT break the model, tests, deck, or demo runner.
# (Streamlit pulls in pyarrow / httptools which lack ARM64 Windows wheels.)
