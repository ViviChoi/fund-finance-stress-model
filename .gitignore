# Local working directory — not part of the public distribution
_private/

# Local Python environment — recreated by ./setup.sh
.venv/

# Python bytecode
__pycache__/
*.py[cod]
*.egg-info/

# Pytest cache
.pytest_cache/

# macOS noise
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editor / IDE
.vscode/
.idea/
*.swp

# Build / packaging
build/
dist/
*.egg

# Notes — generated outputs (figures/, slides/) are kept under version
# control so anyone reading the repo can see what the deliverables look
# like without having to run setup.sh first.
